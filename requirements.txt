# --- Core infra (Phase 0) ---
qdrant-client>=1.12.0      # Qdrant Python client (REST + gRPC, native hybrid/RRF query API)
python-dotenv>=1.0.1       # load GEMINI_API_KEY etc. from .env
pydantic>=2.7.0            # schema definitions (settings + synthesis models)
pydantic-settings>=2.3.0   # typed config loading from env

# --- Ingestion (Phase 1) ---
kaggle>=1.6.14             # pull Cornell arXiv metadata snapshot
google-genai>=1.0.0        # Gemini embeddings + generation (new unified SDK)
fastembed>=0.3.6           # BM25 sparse vectors for the keyword channel
tqdm>=4.66.0               # progress bars for batched embedding

# --- Retrieval (Phase 2) ---
sentence-transformers>=3.0.0   # bge-reranker-v2-m3 cross-encoder (local)
fastapi>=0.110.0               # dev API server for the retrieval visualizer UI
uvicorn>=0.29.0                # ASGI server to run FastAPI

# --- Synthesis (Phase 4) ---
instructor>=1.4.0          # structured outputs (Pydantic) over Gemini

# --- Full-text (Phase 3) ---
requests>=2.32.0           # fetch LaTeX source / PDF from arXiv
